<h2>Create an Account</h2>

<p>By creating an account, you can:</p>
<ul>
  <li>Read entire stories, not just excerpts.</li>
  <li>Upvote the stories you love (and downvote the ones you can live without).</li>
  <li>Submit suggestions to authors for ways they can improve their stories.</li>
  <li>Submit your own stories for review.</li>
</ul>

<form action="/user" method="post">
  <% if @username_error %>
    <p><%= @username_error %></p>
  <% end %>
  <label for="username-main">Username:</label>
  <input type="text" name="username" value="<%= @username %>" id="username-main" required>
  <% if @email_error %>
    <p><%= @email_error %></p>
  <% end %>
  <label for="email-main">Email:</label>
  <input type="email" name="email" value="<%= @email %>" id="email-main" required>
  <% if @password_error %>
    <p><%= @password_error %></p>
  <% end %>
  <label for="password-main">Password:</label>
  <input name="password" type="password" id="password-main" required>

  <button>Sign Up</button>
</form>
